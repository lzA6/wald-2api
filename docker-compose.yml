services:
  nginx:
    image: nginx:latest
    container_name: wald-2api-nginx
    restart: always
    ports:
      - "${NGINX_PORT:-8088}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - wald-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wald-2api-app
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - wald-net

networks:
  wald-net:
    driver: bridge
